# === Python ===
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.installed.cfg
*.egg

# === 虚拟环境 ===
.venv/
venv/
env/

# === IDE ===
.idea/
.vscode/
*.swp
*.swo

# === 模型文件 ===
# 模型使用 Git LFS 管理(见 .gitattributes), 不在此处忽略
# === 构建产物 ===
build/
dist/
*.spec

# === 日志与临时文件 ===
*.log
logs/
temp/

# === AI 配置(含 api key, 不进 git) ===
BsodMoudle/ai_config.json

# === 卡死检测本地配置(用户阈值, 不进 git) ===
FreezeMoudle/freeze_config.json

# === 视频去水印本地配置(不进 git) ===
WatermarkMoudle/watermark_config.json

# === 视频去水印模型(约 200MB, 不进 git, 见 models/README.md) ===
WatermarkMoudle/models/*.onnx

# === 缓存目录(采集的原始图片等,不进 git) ===
cache/

# === 临时测试结果与调试文件 ===
test/a
test/b
out_*.txt
FaceMoudle/liveness/landmark_debug.jpg
